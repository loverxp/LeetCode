// https://leetcode-cn.com/problems/cracking-the-safe/
var Test = require('./Common/Test');

var crackSafe = function (n, k) {

};

function run(n, k) {
    Test.run(crackSafe, n, k);
}

run(2, 4);
// run(4, 5);
// run(4, 10);